# Gin App Scaffold

Minimal Gin framework layout.

Quick start:

1. Change to project directory:

```bash
cd /Users/jagadeeshtakkalaki/Desktop/Gin
```

2. Fetch dependencies:

```bash
go mod tidy
```

3. Run the server:

```bash
PORT=8080 go run ./
```

Visit http://localhost:8080/api/ping
